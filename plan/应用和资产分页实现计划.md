# AppListView 和 AssetsView 分页实现计划

## 分析

参考 ScenesView 的分页实现，需要为以下两个列表页添加分页功能。

---

## 需要修改的文件

### AppListView（应用列表）

| 位置 | 文件 | 改动 |
|------|------|------|
| 后端 | `appController.js` | 支持 page/pageSize 参数 |
| 前端 | `appService.js` | `getAppList` 添加分页参数 |
| 前端 | `AppListView.vue` | 添加分页状态和 UI |

### AssetsView（资产列表）

| 位置 | 文件 | 改动 |
|------|------|------|
| 后端 | `assetController.js` | 支持 page/pageSize 参数 |
| 前端 | `assetService.js` | `getAssets` 添加分页参数 |
| 前端 | `AssetsView.vue` | 添加分页状态和 UI |

---

## 任务清单

### Phase 1: 后端 API 改造

- [ ] 1.1 修改 `appController.js` 的 `getAppList`
- [ ] 1.2 修改 `assetController.js` 的 `getAssets`

### Phase 2: 前端 Service 改造

- [ ] 2.1 修改 `appService.js` 的 `getAppList(page, pageSize)`
- [ ] 2.2 修改 `assetService.js` 的 `getAssets(type, page, pageSize)`

### Phase 3: 前端视图改造

- [ ] 3.1 `AppListView.vue` 添加分页
- [ ] 3.2 `AssetsView.vue` 添加分页

---

## 分页 UI 模板（复用 ScenesView 样式）

```html
<!-- 分页组件 -->
<div class="pagination" v-if="pagination.total > 0">
  <div class="page-size-selector">
    <span>每页</span>
    <select v-model="pagination.pageSize" @change="onPageSizeChange">
      <option :value="5">5</option>
      <option :value="10">10</option>
      <option :value="20">20</option>
      <option :value="30">30</option>
    </select>
    <span>条</span>
  </div>
  <button class="page-btn" :disabled="pagination.page <= 1" @click="goToPage(pagination.page - 1)">
    ‹ 上一页
  </button>
  <div class="page-numbers" v-if="pagination.totalPages > 1">
    <button v-for="p in visiblePages" :key="p" class="page-num" :class="{ active: p === pagination.page }" @click="goToPage(p)">
      {{ p }}
    </button>
  </div>
  <span v-else class="page-num active">1</span>
  <button class="page-btn" :disabled="pagination.page >= pagination.totalPages" @click="goToPage(pagination.page + 1)">
    下一页 ›
  </button>
  <span class="page-info">共 {{ pagination.total }} 条</span>
</div>
```

---

## 分页逻辑模板（复用 ScenesView 代码）

```javascript
// 分页状态
const pagination = ref({
  page: 1,
  pageSize: 10,
  total: 0,
  totalPages: 0
});

// 计算可见页码
const visiblePages = computed(() => {
  const { page, totalPages } = pagination.value;
  const pages = [];
  const maxVisible = 5;
  let start = Math.max(1, page - Math.floor(maxVisible / 2));
  let end = Math.min(totalPages, start + maxVisible - 1);
  if (end - start + 1 < maxVisible) {
    start = Math.max(1, end - maxVisible + 1);
  }
  for (let i = start; i <= end; i++) {
    pages.push(i);
  }
  return pages;
});

const goToPage = (page) => {
  if (page < 1 || page > pagination.value.totalPages) return;
  loadData(page);
};

const onPageSizeChange = () => {
  loadData(1);
};
```
